{% extends 'base.html.twig' %}

{% block title %}Teacher{% endblock %}

{% block body %}
    <h1>Teacher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ teacher.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ teacher.name }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ teacher.age }}</td>
            </tr>
            <tr>
                <th>Salary</th>
                <td>{{ teacher.salary }}</td>
            </tr>
            <tr>
                <th>Majors</th>
                <td>{{ teacher.majors }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ teacher.avatar }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_teacher_index') }}">back to list</a>

    <a href="{{ path('crud_teacher_edit', {'id': teacher.id}) }}">edit</a>

    {{ include('crud_teacher/_delete_form.html.twig') }}
{% endblock %}
