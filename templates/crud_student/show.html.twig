{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>StudentID</th>
                <td>{{ student.studentID }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('crud_student_index') }}">back to list</a>

    <a href="{{ path('crud_student_edit', {'id': student.id}) }}">edit</a>

    {{ include('crud_student/_delete_form.html.twig') }}
{% endblock %}
